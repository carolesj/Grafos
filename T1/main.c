/*
 * Implementação do TAD Grafo
 * */

#include <stdio.h>

int main(int argc, char ** argv) {
    char tipo, representacao;
    int vertices, arestas;
    scanf("%c %c %d %d", &tipo, &representacao, &vertices, &arestas);

    return 0;
}